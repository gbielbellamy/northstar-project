# Copy to .env and paste your own connection string. .env is gitignored.
# Neon gives you this when you create the database; use the pooled URL.
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"
